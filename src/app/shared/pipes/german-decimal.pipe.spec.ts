import { GermanDecimalPipe } from './german-decimal.pipe';

describe('GermanDecimalPipe', () => {
  it('create an instance', () => {
    const pipe = new GermanDecimalPipe();
    expect(pipe).toBeTruthy();
  });
});
