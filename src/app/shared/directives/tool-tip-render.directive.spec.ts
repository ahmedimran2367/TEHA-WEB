/* tslint:disable:no-unused-variable */

import { ToolTipRenderDirective } from './tool-tip-render.directive';

describe('Directive: ToolTipRender', () => {
  it('should create an instance', () => {
    const directive = new ToolTipRenderDirective();
    expect(directive).toBeTruthy();
  });
});
